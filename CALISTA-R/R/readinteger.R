readinteger<-function(){
  #print(paste('Optimal number of cluster according to max. eigenvalue',
  #            toString(x) ))
  #print("If you want to use this value enter 'y' " )
  n=readLines(n=1)   
  #return(as.integer(n))
  return(n)
}